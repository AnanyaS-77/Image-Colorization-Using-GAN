# Image-Colorization-Using-GAN
This project implements a deep learning–based image colorization system using a Generative Adversarial Network (GAN). Grayscale images from the CIFAR-10 dataset are used as inputs, and the generator learns to predict realistic RGB colors while the discriminator evaluates the authenticity of the outputs.
